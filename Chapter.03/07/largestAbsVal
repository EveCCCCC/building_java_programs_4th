public int largestAbsVal(int a, int b, int c){
    a = Math.abs(a);
    b = Math.abs(b);
    c = Math.abs(c);
    if(a >= b && a >= c){
        return a;
    } else if(b > a && b >= c){
        return b;
    } else {
        return c;
    }
}
