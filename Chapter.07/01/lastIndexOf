public int lastIndexOf(int[] array, int x){
    int l = array.length;
    int index = -1;
    for(int i = 0; i < l; i++){
        if(x == array[i]){
            index = i;
        }
    }
    return index;
}
